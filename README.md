# sim docs

Specs for an interactive 3D MIPS datapath + Karnaugh map visualizer. Agent-driven build. Source of truth lives here; code in sibling `sim` repo consumes by name.

Start: `CLAUDE.md`. Reads cross-project foundationals in `book` first, then this repo's foundationals, then ADRs.
